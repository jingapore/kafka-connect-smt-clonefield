package build
import mill.*, kotlinlib.*

object clonefield extends KotlinModule {
  def artifactId = "kafka-connect-smt-clonefield"
  def kotlinVersion = "1.9.24"

  def mvnDeps = Seq(
    mvn"org.apache.kafka:connect-api:4.1.0",
    mvn"org.apache.kafka:connect-transforms:4.1.0"
  )

  object test extends KotlinTests, TestModule.Junit5 {
    def mvnDeps = Seq(
      mvn"io.kotest:kotest-runner-junit5:5.9.1"
    )
  }
}
